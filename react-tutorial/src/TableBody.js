import React, {Component} from 'react';

const TableBody = props =>{
    const rows = props.characterData.map((riw,index)=>{
        
    }
    
    
    );

    return(
        <tbody> 
           
        </tbody>
    );
}

export default TableBody;